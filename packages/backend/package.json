{
  "name": "@nihalgonsalves/expenses-backend",
  "description": "",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "run-p 'build --watch' 'start'",
    "start": "node --watch ./dist/app.js",
    "build": "esbuild --platform=node --bundle ./src/app.ts --outdir=./dist/"
  },
  "dependencies": {
    "@trpc/server": "^10.34.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "esbuild": "^0.18.11",
    "vitest": "*"
  }
}
