import { describe, it } from 'vitest';

describe('NotificationService', () => {
  describe('sendNotifications', () => {
    it.todo('sends notifications to all users');

    it.todo('unsubscribes on failure (e.g. 410 Gone)');
  });
});
