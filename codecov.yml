flags:
  backend:
    paths:
      - packages/backend/
  frontend:
    paths:
      - packages/frontend/
  shared:
    paths:
      - packages/shared/

coverage:
  status:
    project: off
    patch: off
